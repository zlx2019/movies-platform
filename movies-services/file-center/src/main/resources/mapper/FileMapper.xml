<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movies.file.mapper.FileMapper">

    <select id="findList" resultType="com.movies.common.model.File">
        select * from movies_file ORDER BY create_time DESC
    </select>

</mapper>